[project]
name = "fishing-price-tracker"
version = "0.1.0"
description = "Fishing gear deep-discount deal tracker: scraper, deal engine, and CLI."
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
